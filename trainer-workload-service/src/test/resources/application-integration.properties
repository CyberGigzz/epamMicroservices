# Integration Test Profile Configuration
# MongoDB and ActiveMQ are configured dynamically via Testcontainers

spring.application.name=trainer-workload-service-integration

# Logging
logging.level.com.gym.trainer_workload_service=DEBUG
logging.level.org.springframework.jms=DEBUG
